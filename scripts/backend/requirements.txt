fastapi==0.136.0
uvicorn[standard]==0.44.0
pydantic-settings==2.13.1
python-dotenv==1.2.2
httpx==0.28.1

# HTTP retry
tenacity==9.1.4

# WhatsApp + güvenlik
pyotp==2.9.0
cryptography==46.0.7

# Zamanlama
apscheduler==3.11.2
SQLAlchemy==2.0.49

# PDF parse
pymupdf==1.27.2.2

# Tarayıcı otomasyonu (FEAT-13)
playwright==1.58.0

# AT-SPI accessibility tree (FEAT-17)
# PyGObject (gi) sistem paketiyle sağlanır: sudo apt install python3-gi gir1.2-atspi-2.0
# venv/pyvenv.cfg → include-system-site-packages = true (zaten ayarlı)

# Tarih parse (Türkçe)
dateparser==1.4.0

# Video işleme
ffmpeg-python==0.2.0

# X11 in-process giriş (DESK-OPT-2) — xdotool type subprocess'ini elimine eder
python-xlib>=0.33

# X11 ekran yakalama (DESK-OPT-3) — scrot subprocess'ini elimine eder; X11 SHM ile <50ms
mss>=9.0

# Görüntü işleme — screenshot resize (vision API boyut limiti için)
Pillow>=10.0

# Loglama
python-json-logger==3.3.0
